name: CI/CD AniAlert Bot Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      EC2_USER: ${{ secrets.EC2_USER }}
      EC2_HOST: ${{ secrets.EC2_HOST }}
      DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
      CLIENT_ID: ${{ secrets.CLIENT_ID }}
      HEADER: ${{ secrets.HEADER }}
      DB_TYPE: ${{ secrets.DB_TYPE }}
      DB_HOST: ${{ secrets.DB_HOST }}
      DB_NAME: ${{ secrets.DB_NAME }}
      DB_USER: ${{ secrets.DB_USER }}
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      DB_PORT: ${{ secrets.DB_PORT }}
      REDIS_HOST: ${{ secrets.REDIS_HOST }}
      REDIS_PORT: ${{ secrets.REDIS_PORT }}
      REDIS_USE_TLS: ${{ secrets.REDIS_USE_TLS }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Prepare SSH key
        run: |
          echo "${{ secrets.EC2_KEY }}" > ec2_key.pem
          chmod 600 ec2_key.pem

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ env.EC2_HOST }}
          username: ${{ env.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            set -ex

            echo "==== [Ensure AniAlert repo exists and is clean] ===="
            if [ ! -d ~/AniAlert/.git ]; then
              echo "Repository broken or missing. Re-cloning AniAlert..."
              rm -rf ~/AniAlert
              git clone https://github.com/ganraoyu/AniAlert.git ~/AniAlert
            fi

            cd ~/AniAlert || { echo "cd failed"; exit 1; }

            echo "Resetting repository and pulling latest code..."
            git reset --hard
            timeout 60s git pull origin main

            echo "==== [Setup Python venv and install] ===="
            if [ ! -d venv ]; then
              python3 -m venv venv
            fi

            source venv/bin/activate
            pip install --upgrade pip setuptools wheel
            sudo apt-get update
            sudo apt-get install -y libpq-dev
            pip install -e .

            echo "==== [Install screen and restart bot] ===="
            sudo apt install -y screen
            pkill -f "python -u AniAlert/bot.py" || true
            screen -dmS anialert_session bash -c "source ~/AniAlert/venv/bin/activate && python -u AniAlert/bot.py"
